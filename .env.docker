# Variables de entorno para Docker
# Cambiar 'backend' por la IP o dominio si se despliega en diferentes servidores

# Para docker-compose (comunicación entre contenedores)
VITE_API_URL=http://backend:3000

# Para despliegue en diferentes servidores
# VITE_API_URL=https://tu-backend.com
